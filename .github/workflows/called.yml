name: GR Mailsource

on:
  workflow_call:
    inputs:
      GAROON_VERSION:
        required: true
        type: string

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          ref: test-branch
      - run: echo "${{ inputs.GAROON_VERSION }}"
